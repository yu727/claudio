# Task: Add Left/Right Sidebars to Player Page

## Goal
Restructure PlayerPage to have two collapsible sidebars inside the player card:
- **Left sidebar**: AI Chat (ChatArea + IntentInput) — toggle with a button on the left edge
- **Right sidebar**: Song Selection (SearchPanel + QueueList + visual mode selector) — toggle with a button on the right edge

## Current Structure
PlayerPage.tsx layout (top to bottom):
1. AudioVisualizer + FluidBlobs + BorderGlow (backgrounds)
2. `.player-card`:
   - `.player-upper`: DJ header + AudioSpectrum
   - `.player-lower`: player-content (cover + lyrics), progress bar, controls
   - WaveformBar
3. SpectrumBars
4. `.chat-section`: ChatArea + IntentInput (currently below player card)
5. `.search-toggle` button + SearchPanel
6. `.extras-toggle` button + extras-panel (visual mode + queue)

## Target Layout
```
.player-card
├── .player-upper (keep as-is)
├── .player-main-area (NEW flex row container)
│   ├── .sidebar-left (collapsible, 300px wide when open)
│   │   ├── .sidebar-toggle-btn (◀/▶ icon on left edge)
│   │   ├── .sidebar-header ("AI 对话")
│   │   ├── ChatArea
│   │   └── IntentInput
│   ├── .player-lower (existing content, flex: 1)
│   │   ├── .player-content (cover + lyrics, keep as-is)
│   │   ├── autoplay-banner, error-banner
│   │   ├── progress-row
│   │   └── controls-row
│   └── .sidebar-right (collapsible, 300px wide when open)
│       ├── .sidebar-toggle-btn (◀/▶ icon on right edge)
│       ├── .sidebar-header ("歌曲选择")
│       ├── SearchPanel (inline, no separate toggle)
│       ├── QueueList
│       └── Visual Mode selector (theme-grid)
├── WaveformBar
```

## CSS Requirements

### .player-main-area
```css
.player-main-area {
  display: flex;
  flex-direction: row;
  width: 100%;
  min-height: 0;
  position: relative;
  overflow: hidden;
}
```

### .sidebar-left / .sidebar-right
```css
.sidebar-left, .sidebar-right {
  display: flex;
  flex-direction: column;
  width: 0;
  min-width: 0;
  overflow: hidden;
  transition: width 0.35s cubic-bezier(0.4, 0, 0.2, 1), 
              min-width 0.35s cubic-bezier(0.4, 0, 0.2, 1),
              opacity 0.25s ease;
  opacity: 0;
  background: var(--bg-glass);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border: 1px solid var(--border);
  border-radius: var(--radius-md);
  flex-shrink: 0;
}

.sidebar-left.open, .sidebar-right.open {
  width: 320px;
  min-width: 320px;
  opacity: 1;
  margin: 0 12px;
}
```

### .sidebar-toggle-btn
```css
.sidebar-toggle-btn {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  width: 24px;
  height: 48px;
  border: 1px solid var(--border);
  background: var(--bg-glass);
  backdrop-filter: blur(12px);
  color: var(--text-secondary);
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 12px;
  z-index: 10;
  transition: all 0.2s;
  border-radius: 4px;
}

.sidebar-toggle-btn:hover {
  background: var(--bg-card);
  color: var(--text-primary);
}

/* Left toggle: positioned on the right edge of left sidebar, or at left edge of player-lower when sidebar is closed */
.sidebar-left .sidebar-toggle-btn {
  right: -24px;
  border-left: none;
  border-radius: 0 4px 4px 0;
}

.sidebar-right .sidebar-toggle-btn {
  left: -24px;
  border-right: none;
  border-radius: 4px 0 0 4px;
}
```

### sidebar-header
```css
.sidebar-header {
  padding: 12px 16px;
  font-size: 14px;
  font-weight: 600;
  color: var(--text-primary);
  border-bottom: 1px solid var(--border);
  display: flex;
  align-items: center;
  gap: 8px;
  flex-shrink: 0;
}
```

### sidebar-content
```css
.sidebar-content {
  flex: 1;
  overflow-y: auto;
  min-height: 0;
  padding: 8px;
}
```

### Remove old styles
- `.chat-section` — remove standalone styling (move content into sidebar)
- `.search-toggle` — remove (integrated into sidebar)
- `.extras-toggle` — remove (integrated into sidebar)  
- `.extras-panel` — remove standalone (content moves into sidebar)

## PlayerPage.tsx Changes

### New state
```tsx
const [showLeftSidebar, setShowLeftSidebar] = useState(false);
const [showRightSidebar, setShowRightSidebar] = useState(false);
```

### Remove from JSX
- The entire `.chat-section` div (lines 341-344)
- The `.search-toggle` div (lines 347-351)
- The `showSearch` state and conditional SearchPanel rendering (lines 52, 353-358)
- The `.extras-toggle` div (lines 361-365)
- The `showExtras` state and conditional extras-panel (lines 49, 367-398)

### New JSX structure inside `.player-card`, between `.player-upper` and `WaveformBar`:
```tsx
<div className="player-main-area">
  {/* Left Sidebar - AI Chat */}
  <div className={`sidebar-left ${showLeftSidebar ? 'open' : ''}`}>
    <div className="sidebar-header">
      <span>🤖</span> AI 对话
      <button className="sidebar-close-btn" onClick={() => setShowLeftSidebar(false)}>✕</button>
    </div>
    <div className="sidebar-content">
      <ChatArea />
    </div>
    <IntentInput />
  </div>

  {/* Toggle buttons (always visible, positioned relative to main-area) */}
  {!showLeftSidebar && (
    <button 
      className="sidebar-float-btn left" 
      onClick={() => setShowLeftSidebar(true)}
      title="AI 对话"
    >
      ▶
    </button>
  )}

  {/* Center: Player content */}
  <div className="player-lower">
    {/* ... existing player-lower content ... */}
  </div>

  {!showRightSidebar && (
    <button 
      className="sidebar-float-btn right" 
      onClick={() => setShowRightSidebar(true)}
      title="歌曲选择"
    >
      ◀
    </button>
  )}

  {/* Right Sidebar - Song Selection */}
  <div className={`sidebar-right ${showRightSidebar ? 'open' : ''}`}>
    <div className="sidebar-header">
      <span>🎵</span> 歌曲选择
      <button className="sidebar-close-btn" onClick={() => setShowRightSidebar(false)}>✕</button>
    </div>
    <div className="sidebar-content">
      {/* Visual Mode Selector */}
      <div className="section-label">VISUAL MODE</div>
      <div className="theme-grid">
        {VISUAL_MODES.map((m) => (
          <button key={m.key} className={`theme-btn ${visualMode === m.key ? 'active' : ''}`} onClick={() => setVisualMode(m.key)}>
            {m.name}
          </button>
        ))}
      </div>
      
      {/* Search */}
      <SearchPanel onSelectSong={(item) => { playItem(item); }} />
      
      {/* Queue */}
      {queue.length > 0 && (
        <div className="queue-section">
          <div className="queue-header">
            <div className="section-label">{t("queueTitle")} ({queue.length})</div>
            <button className="btn-save-playlist" onClick={() => setShowSaveDialog(true)}>
              {t("saveAsPlaylist")}
            </button>
          </div>
          <QueueList items={queue} onItemClick={playItem} />
        </div>
      )}
    </div>
  </div>
</div>
```

### Float buttons CSS
```css
.sidebar-float-btn {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  width: 24px;
  height: 60px;
  border: 1px solid var(--border);
  background: var(--bg-glass);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  color: var(--text-secondary);
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 11px;
  z-index: 10;
  transition: all 0.2s;
  border-radius: 4px;
  opacity: 0.6;
}

.sidebar-float-btn:hover {
  opacity: 1;
  background: var(--bg-card);
  color: var(--text-primary);
}

.sidebar-float-btn.left {
  left: 0;
  border-left: none;
  border-radius: 0 4px 4px 0;
}

.sidebar-float-btn.right {
  right: 0;
  border-right: none;
  border-radius: 4px 0 0 4px;
}
```

### sidebar-close-btn
```css
.sidebar-close-btn {
  margin-left: auto;
  background: none;
  border: none;
  color: var(--text-secondary);
  cursor: pointer;
  font-size: 14px;
  padding: 4px;
  border-radius: 4px;
  transition: color 0.2s;
}

.sidebar-close-btn:hover {
  color: var(--text-primary);
}
```

### Responsive (mobile < 768px)
On mobile, sidebars should overlay (absolute positioning) instead of pushing content:
```css
@media (max-width: 768px) {
  .sidebar-left.open, .sidebar-right.open {
    position: absolute;
    top: 0;
    bottom: 0;
    width: 85vw;
    min-width: 85vw;
    max-width: 85vw;
    z-index: 100;
    margin: 0;
  }
  .sidebar-left.open {
    left: 0;
  }
  .sidebar-right.open {
    right: 0;
  }
}
```

## Important Notes
- Do NOT touch any component internals (ChatArea, IntentInput, SearchPanel, QueueList, KaraokeLyrics etc.)
- Do NOT touch AudioPlayer, playerStore, chatStore
- Do NOT touch visualizer components
- Do NOT touch route config or server code
- Keep all existing functionality working
- The `showSearch` state can be removed since SearchPanel is always visible in right sidebar
- The `showExtras` state can be removed since extras content moves to right sidebar
- Keep `showSaveDialog` and the save dialog modal as-is
- Keep DjPanel and UserPanel as-is (modal overlays, not affected)

## Files to Modify
1. `/projects/Claudio/apps/web/src/pages/PlayerPage.tsx` — restructure JSX layout
2. `/projects/Claudio/apps/web/src/styles/global.css` — add sidebar CSS styles, remove old chat-section/extras styles

## Build & Verify
After changes:
1. Run `pnpm --filter @ai-radio/web build` to verify no errors
2. Ensure dev server HMR picks up changes
